# .github/workflows/manual-inbox-summary.yml

name: Manual Inbox Summary Notifier

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for running the workflow'
        required: false
        default: 'Checking inbox contents'

jobs:
  send-inbox-summary:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare and Send Summary
        env:
          BOT_TOKEN: ${{ secrets.INBOX_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.INBOX_CHAT_ID }}
        run: |
          if [ ! -d "_inbox" ]; then
            MESSAGE="Your Cortagora repository does not have an _inbox folder yet."
          else
            # We will generate the body of the message inside a command substitution to avoid the subshell trap.
            FORMATTED_LIST=$(
              # This variable is now local to the subshell, which is fine.
              CURRENT_MONTH_HEADER=""

              # The find and sort commands now pipe directly into the while loop.
              find _inbox -maxdepth 1 -mindepth 1 -printf "%TY-%Tm|%f\0" | sort -rz | \
              while IFS='|' read -r -d '' month_key filename; do
                NEW_MONTH_HEADER=$(date -d "${month_key}-01" "+*%-B %Y*")
                
                if [ "$NEW_MONTH_HEADER" != "$CURRENT_MONTH_HEADER" ]; then
                  # Add a blank line for spacing between month groups.
                  # The first group will have a leading blank line, which is acceptable.
                  echo ""
                  echo "$NEW_MONTH_HEADER"
                  CURRENT_MONTH_HEADER="$NEW_MONTH_HEADER"
                fi

                if [ -d "_inbox/$filename" ]; then
                  echo "- \`$filename (folder)\`"
                else
                  echo "- \`$filename\`"
                fi
              done
            )

            if [ -z "$FORMATTED_LIST" ]; then
              MESSAGE="✅ Your Cortagora inbox is empty. Great job!"
            else
              HEADER=$'📬 *Cortagora Inbox Triage*\n\nYou have the following items to process:'
              MESSAGE="${HEADER}${FORMATTED_LIST}"
            fi
          fi

          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
          --data-urlencode "chat_id=${CHAT_ID}" \
          --data-urlencode "parse_mode=Markdown" \
          --data-urlencode "text=${MESSAGE}"