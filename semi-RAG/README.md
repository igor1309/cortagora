# The workshop for curated, temporary ticket contexts

For each new task, create a dedicated folder inside `/semi-RAG/`. The name should describe the ticket (e.g., `ticket-004-act1-inciting-incident`).
